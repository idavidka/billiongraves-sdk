# BillionGraves SDK - API Reference

> **TODO**: This document is a placeholder. It will be written once the
> API endpoints have been determined from HAR file analysis.

---

## Overview

This document will describe all BillionGraves API endpoints used by the SDK,
including request/response shapes, query parameters, and example calls.

## Planned Sections

### Grave Records

- `GET /api/graves/search` — Search grave records
- `GET /api/graves/:id` — Get a single grave record

### Cemeteries

- `GET /api/cemeteries/search` — Search cemeteries
- `GET /api/cemeteries/:id` — Get a single cemetery

---

*Last updated: 2026-03-10 — skeleton only*
